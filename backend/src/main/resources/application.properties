spring.application.name=tfg

# Riot API key placeholder - set this to your Riot API key if you plan to call the Riot API
riot.api.key=
 
# JWT signing secret used to sign and verify tokens. In production, set this to a strong, secret value
# For development you can use a short placeholder, but keep this stable across restarts.
jwt.secret=change-me-dev-secret-please-replace
